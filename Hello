-- Example of script that behaves differently depending on how it's executed

if not _G.isLoadedFromString then
    error("This script should be executed via loadstring()")
else
    -- Your code here that should only run when loaded by loadstring()
    print("Script executed via loadstring() from GitHub!")
end